<div class="container-fluid mt-4">
    <div class="flex-row">
        <div class="col-12">
            <h1>Владельцы сервисов</h1>
            <div class="card mt-3">
                <div class="card-header">
                    <div class="body-panel-buttons">
                        <button id="btn-page-data" type="button" class="btn btn-sm btn-success" onclick="window.location.href='<%= new_owner_path %>'">
                            Добавить
                        </button>
                    </div>
                </div>
                <div class="card-body">
                    <% if @owners.count > 0 %>
                        <table class="table table-striped">
                            <thead class="thead-dark">
                                <tr><th>Название</th></tr>
                            </thead>
                            <tbody>
                                <% @owners.each do |owner| %>
                                    <tr>
                                        <td><%= link_to owner.name, edit_owner_path(owner) %></td>
                                    </tr>
                                <% end %>
                            </tbody>
                        </table>
                    <% else %>
                        <p><h5 class="emptyData">Данные отсутствуют</h5></p>
                    <% end %>
                </div>
            </div>

        </div>
    </div>
</div>